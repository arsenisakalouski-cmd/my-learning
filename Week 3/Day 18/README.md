# День 18: Flask - Аутентификация пользователей

## 🎯 Что изучили:

### 1. Cookies
- Что такое cookies
- Как устанавливать и читать
- Срок жизни cookies
- Примеры использования

### 2. Sessions
- Что такое сессии
- Отличия от cookies
- Работа с session в Flask
- Хранение данных пользователя

### 3. Хеширование паролей
- Почему нельзя хранить пароли открытым текстом
- Библиотека bcrypt
- Соль (salt) для безопасности
- Проверка паролей

### 4. Система аутентификации
- Регистрация пользователей
- Вход/Выход (Login/Logout)
- Защита страниц декораторами
- Связь пользователей с контентом

### 5. Декораторы для защиты
- `@login_required` - требует авторизации
- `@logout_required` - только для неавторизованных
- `@author_required` - только автор может редактировать

## 📁 Структура проекта:
```
Day 18/
├── app.py                      # Flask приложение с аутентификацией
├── database.py                 # Функции работы с БД + пользователи
├── auth_decorators.py          # Декораторы защиты страниц
├── blog.db                     # База данных
├── 01_cookies_example.py       # Примеры cookies
├── 02_sessions_example.py      # Примеры сессий
├── 03_password_hashing.py      # Хеширование паролей
├── 04_user_system.py           # Простая система пользователей
├── test_auth.py                # Тестирование
├── templates/
│   ├── base.html              # Обновлённая навигация
│   ├── index.html
│   ├── post.html              # Кнопки только для автора
│   ├── new_post.html          # Без поля author
│   ├── edit_post.html
│   ├── search.html
│   ├── stats.html
│   ├── register.html          # НОВЫЙ - регистрация
│   ├── login.html             # НОВЫЙ - вход
│   └── profile.html           # НОВЫЙ - профиль
└── static/
    └── css/
        └── blog.css           # Обновлённые стили
```

## 🗄️ Обновлённая структура БД:

### Таблица: users (НОВАЯ)
```sql
CREATE TABLE users (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    username TEXT UNIQUE NOT NULL,
    email TEXT UNIQUE NOT NULL,
    password_hash TEXT NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### Таблица: posts (ОБНОВЛЁННАЯ)
```sql
CREATE TABLE posts (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    title TEXT NOT NULL,
    content TEXT NOT NULL,
    author TEXT NOT NULL,
    author_id INTEGER,                    -- НОВОЕ
    views INTEGER DEFAULT 0,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (author_id) REFERENCES users (id)  -- НОВОЕ
);
```

## 🚀 Как запустить:
```bash
# Установить bcrypt
pip install bcrypt --break-system-packages

# Тестирование
python test_auth.py

# Запуск блога
python app.py

# Примеры
python 01_cookies_example.py    # http://localhost:5005/
python 02_sessions_example.py   # http://localhost:5006/
```

**Откройте:** http://localhost:5007/

## 📚 Ключевые концепции:

### Cookies:
```python
# Установить
response = make_response("...")
response.set_cookie('name', 'value', max_age=3600)

# Прочитать
value = request.cookies.get('name')

# Удалить
response.delete_cookie('name')
```

### Sessions:
```python
# Установить секретный ключ
app.secret_key = 'random-secret-key'

# Записать
session['user_id'] = 123
session['username'] = 'Ivan'

# Прочитать
user_id = session.get('user_id')

# Удалить
session.clear()
```

### Хеширование:
```python
import bcrypt

# Хеширование
password_bytes = password.encode('utf-8')
salt = bcrypt.gensalt()
hashed = bcrypt.hashpw(password_bytes, salt)

# Проверка
is_correct = bcrypt.checkpw(password_bytes, hashed)
```

### Декораторы:
```python
from auth_decorators import login_required

@app.route('/protected')
@login_required
def protected():
    return "Только для авторизованных"
```

### Регистрация:
```python
@app.route('/register', methods=['POST'])
def register():
    username = request.form.get('username')
    password = request.form.get('password')
    
    # Хешировать пароль
    hashed = hash_password(password)
    
    # Сохранить в БД
    save_user(username, hashed)
    
    return redirect(url_for('login'))
```

### Вход:
```python
@app.route('/login', methods=['POST'])
def login():
    username = request.form.get('username')
    password = request.form.get('password')
    
    # Найти пользователя
    user = get_user(username)
    
    # Проверить пароль
    if check_password(password, user['password_hash']):
        # Сохранить в сессию
        session['user_id'] = user['id']
        session['username'] = user['username']
        return redirect(url_for('index'))
```

### Выход:
```python
@app.route('/logout')
def logout():
    session.clear()
    return redirect(url_for('index'))
```

## 🔗 Основные страницы:

- `/register` - Регистрация
- `/login` - Вход
- `/logout` - Выход
- `/profile` - Профиль (только авторизованные)
- `/post/new` - Создать пост (только авторизованные)
- `/post/<id>/edit` - Редактировать (только автор)
- `/post/<id>/delete` - Удалить (только автор)

## 💡 Новые возможности:

### 1. Система пользователей
```
БЫЛО:
Любой может редактировать/удалять посты

СТАЛО:
Только автор может редактировать свои посты
```

### 2. Защита страниц
```python
# Создание поста - только авторизованные
@app.route('/post/new')
@login_required
def new_post():
    ...

# Редактирование - только автор
@app.route('/post/<id>/edit')
@login_required
@author_required
def edit_post(post_id):
    ...
```

### 3. Связь контента с пользователями
```sql
-- Пост привязан к пользователю
posts.author_id → users.id

-- Можно узнать:
- Кто создал пост
- Сколько постов у пользователя
- Все посты пользователя
```

### 4. Профиль пользователя
```
Показывает:
- Имя пользователя
- Email
- Дату регистрации
- Статистику (посты, просмотры)
- Список своих постов
```

## 🔐 Безопасность:

### Что мы реализовали:
✅ Пароли хешируются (bcrypt)
✅ Соль для каждого пароля
✅ Проверка авторства
✅ Защита страниц декораторами
✅ Валидация данных
✅ Уникальность username/email

### Что ещё нужно для продакшена:
- [ ] HTTPS (SSL сертификат)
- [ ] CSRF защита
- [ ] Rate limiting (ограничение запросов)
- [ ] Email подтверждение
- [ ] Восстановление пароля
- [ ] Двухфакторная аутентификация (2FA)
- [ ] Капча

## 🎓 Следующий шаг:

День 19 - API разработка (REST API, JSON, Postman)

## 🏆 Достижения:

- ✅ Изучили cookies и sessions
- ✅ Реализовали хеширование паролей
- ✅ Создали систему регистрации
- ✅ Добавили вход/выход
- ✅ Защитили страницы декораторами
- ✅ Связали посты с пользователями
- ✅ Создали профиль пользователя

## 📝 Примеры использования:

### Зарегистрироваться:
1. Открыть /register
2. Заполнить форму
3. Нажать "Зарегистрироваться"

### Войти:
1. Открыть /login
2. Ввести логин/пароль
3. Нажать "Войти"

### Создать пост:
1. Войти в систему
2. Нажать "+ Новый пост"
3. Заполнить форму
4. Опубликовать

### Редактировать свой пост:
1. Открыть свой пост
2. Нажать "✏️ Редактировать"
3. Изменить
4. Сохранить

### Попытка редактировать чужой пост:
1. Открыть чужой пост
2. Кнопки редактирования НЕ ВИДНЫ
3. Прямой доступ к URL → ошибка "Только автор может редактировать"

## 🐛 Возможные проблемы:

### Ошибка: ModuleNotFoundError: No module named 'bcrypt'
```bash
pip install bcrypt --break-system-packages
```

### Ошибка: "secret_key not set"
```python
# Добавить в app.py
app.secret_key = 'your-secret-key'
```

### Пароли не работают после миграции
```python
# Удалить старую БД и создать заново
import os
os.remove('blog.db')
python app.py
```