# День 22: Matplotlib - визуализация данных

## 🎯 Что изучили:

### 1. Основы Matplotlib
- Создание графиков
- Стили линий, цвета, маркеры
- Подписи, легенды, сетки
- Настройка размеров

### 2. Типы графиков
- Line plots (линейные)
- Bar charts (столбчатые)
- Histograms (гистограммы)
- Scatter plots (рассеяния)
- Pie charts (круговые)

### 3. Субплоты
- Создание сеток графиков
- Индексация субплотов
- GridSpec для гибкости
- Общие оси и подписи

### 4. Настройка
- Стили (seaborn, ggplot)
- Цветовые палитры
- Шрифты и размеры
- Аннотации и стрелки
- Заполнение областей

### 5. Сохранение
- Различные форматы (PNG, PDF, SVG)
- Разрешение (DPI)
- Прозрачность
- Оптимизация размера

### 6. Практические примеры
- Дашборды продаж
- Воронки конверсии
- KPI карточки
- Прогнозы

## 📁 Файлы:
```
Day 22/
├── 01_matplotlib_basics.py      # Основы
├── 02_plot_types.py            # Типы графиков
├── 03_subplots.py              # Субплоты
├── 04_customization.py         # Настройка
├── 05_saving_plots.py          # Сохранение
├── 06_practical_examples.py    # Практика
├── plots/                      # Сохранённые графики
└── README.md                   # Этот файл
```

## 🚀 Установка:
```bash
pip install matplotlib pandas --break-system-packages
```

## 📚 Ключевые функции:

### Базовые:
```python
plt.plot(x, y)              # Линейный
plt.bar(x, y)               # Столбчатый
plt.hist(data, bins=30)     # Гистограмма
plt.scatter(x, y)           # Рассеяние
plt.pie(sizes, labels=...)  # Круговой
```

### Настройка:
```python
plt.title('Заголовок')
plt.xlabel('X'), plt.ylabel('Y')
plt.legend()
plt.grid(True)
plt.xlim(0, 10), plt.ylim(0, 100)
```

### Субплоты:
```python
fig, ax = plt.subplots(nrows, ncols)
fig, (ax1, ax2) = plt.subplots(1, 2)
fig, axes = plt.subplots(2, 3)
```

### Сохранение:
```python
plt.savefig('file.png')
plt.savefig('file.pdf', dpi=300)
plt.savefig('file.png', bbox_inches='tight', transparent=True)
```

## 💡 Когда использовать:

- **Line Plot** - тренды, временные ряды
- **Bar Chart** - сравнение категорий
- **Histogram** - распределение данных
- **Scatter** - зависимость переменных
- **Pie Chart** - доли целого

## 🎨 Стили:
```python
plt.style.use('seaborn-v0_8')
plt.style.use('ggplot')
plt.style.use('fivethirtyeight')
```

## 🎓 Следующий шаг:

День 23 - Seaborn: статистическая визуализация