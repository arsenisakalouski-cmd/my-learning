# День 21: Pandas - работа с табличными данными

## 🎯 Что изучили:

### 1. Основы Pandas
- Series (одномерные данные)
- DataFrame (таблицы)
- Создание, доступ, фильтрация
- Сортировка и операции

### 2. Чтение/запись данных
- CSV файлы
- Excel (xlsx)
- JSON
- Преобразования

### 3. Группировка и агрегация
- groupby()
- agg() - множественные агрегации
- pivot_table() - сводные таблицы
- transform() и apply()

### 4. Пропущенные данные
- Обнаружение (isnull, notnull)
- Удаление (dropna)
- Заполнение (fillna)
- Интерполяция

### 5. Объединение данных
- merge() - SQL-стиль join
- concat() - склейка
- join() - по индексу
- Типы: inner, left, right, outer

### 6. Практические задачи
- Анализ продаж
- Очистка данных
- Временные ряды
- Объединение источников

## 📁 Файлы:
```
Day 21/
├── 01_pandas_basics.py         # Основы Pandas
├── 02_pandas_data_io.py        # Чтение/запись
├── 03_pandas_groupby.py        # Группировка
├── 04_pandas_missing_data.py   # Пропуски
├── 05_pandas_merging.py        # Объединение
├── 06_pandas_practice.py       # Практика
└── README.md                   # Этот файл
```

## 🚀 Установка:
```bash
pip install pandas openpyxl --break-system-packages
```

## 📚 Ключевые функции:

### Создание:
```python
pd.Series([1, 2, 3])
pd.DataFrame({'A': [1, 2], 'B': [3, 4]})
pd.read_csv('file.csv')
pd.read_excel('file.xlsx')
```

### Просмотр:
```python
df.head()         # Первые строки
df.tail()         # Последние строки
df.info()         # Информация
df.describe()     # Статистика
df.shape          # Размер
```

### Доступ:
```python
df['column']                    # Столбец
df[['col1', 'col2']]           # Несколько
df.loc[0]                      # Строка
df.iloc[0]                     # По позиции
df.loc[0, 'column']            # Ячейка
```

### Фильтрация:
```python
df[df['Age'] > 25]                      # Условие
df[(df['Age'] > 25) & (df['City'] == 'Moscow')]  # И
df[df['City'].isin(['Moscow', 'SPB'])]  # В списке
```

### Группировка:
```python
df.groupby('column')['value'].mean()
df.groupby('col').agg(['sum', 'mean'])
pd.pivot_table(df, values='A', index='B', columns='C')
```

### Пропуски:
```python
df.isnull()                    # Проверка
df.dropna()                    # Удалить
df.fillna(0)                   # Заполнить
df.interpolate()               # Интерполяция
```

### Объединение:
```python
pd.merge(df1, df2, on='key')              # Inner join
pd.merge(df1, df2, on='key', how='left')  # Left join
pd.concat([df1, df2])                     # Вертикально
pd.concat([df1, df2], axis=1)             # Горизонтально
```

## 💡 Применение:

- **Data Science** - обработка и анализ данных
- **Business Analytics** - отчёты, дашборды
- **Machine Learning** - подготовка данных
- **Finance** - анализ временных рядов
- **Research** - обработка исследований

## 🎓 Следующий шаг:

День 22 - Matplotlib: визуализация данных