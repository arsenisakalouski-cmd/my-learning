# 🤖 Машинное обучение - Шпаргалка

## 📝 Суть ML одной фразой:

**Компьютер учится на примерах и находит закономерности**

## 🎯 Два главных типа:

### 1. РЕГРЕССИЯ (предсказать ЧИСЛО)
```python
from sklearn.linear_model import LinearRegression

model = LinearRegression()
model.fit(X_train, y_train)      # Учим
predictions = model.predict(X_test)  # Предсказываем
```

**Примеры:**
- Цена дома
- Температура завтра
- Зарплата через 5 лет

### 2. КЛАССИФИКАЦИЯ (предсказать КАТЕГОРИЮ)
```python
from sklearn.linear_model import LogisticRegression

model = LogisticRegression()
model.fit(X_train, y_train)      # Учим
predictions = model.predict(X_test)  # Предсказываем
```

**Примеры:**
- Спам или не спам
- Кошка или собака  
- Одобрить кредит (да/нет)

## 🔑 5 обязательных шагов:
```python
# 1. Подготовить данные
X = данные_признаки
y = данные_цели

# 2. Разделить
X_train, X_test, y_train, y_test = train_test_split(X, y)

# 3. Создать модель
model = LinearRegression()

# 4. Обучить
model.fit(X_train, y_train)

# 5. Предсказать и оценить
predictions = model.predict(X_test)
score = model.score(X_test, y_test)
```

## 📊 Метрики:

**Регрессия:**
- R² (0-1, лучше → 1)
- RMSE (меньше → лучше)

**Классификация:**
- Accuracy (0-100%, больше → лучше)

## ⚠️ Главные ошибки новичков:

1. ❌ Не разделять на train/test
2. ❌ Оценивать на обучающих данных
3. ❌ Забывать reshape(-1, 1) для X
4. ❌ Не визуализировать результаты