# День 25: Продвинутое машинное обучение

## 🎯 Что изучили:

### 1. Деревья решений (Decision Trees)
- Как работают (блок-схема вопросов)
- DecisionTreeRegressor / DecisionTreeClassifier
- Параметр max_depth (глубина дерева)
- Визуализация структуры дерева
- Преимущества: понятно, работает с нелинейностью
- Недостатки: переобучение, нестабильность

### 2. Random Forest (Случайный лес)
- Много деревьев вместе = стабильнее
- Усреднение предсказаний
- Параметры: n_estimators, max_depth
- Feature importance (важность признаков)
- Лучше чем одно дерево

### 3. Feature Engineering (Создание признаков)
- Создание новых признаков из существующих
- Комбинации: деление, умножение
- Преобразования: возраст из года
- Категории из чисел: высокий/низкий
- One-Hot Encoding для категорий
- Улучшение качества на 10-30%

### 4. Cross-Validation (Кросс-валидация)
- K-Fold разделение данных
- Многократное тестирование
- Более надёжная оценка качества
- cross_val_score()
- Обычно cv=5 или cv=10

### 5. Финальный проект
- Полный цикл ML проекта
- Предсказание цен автомобилей
- От данных до применения

## 📁 Файлы:
```
Day 25/
├── 01_decision_tree.py          # Деревья решений
├── 02_random_forest_simple.py   # Random Forest
├── 03_feature_engineering.py    # Создание признаков
├── 04_cross_validation.py       # Кросс-валидация
├── 05_final_project.py          # Финальный проект
└── README.md                    # Этот файл
```

## 🌳 Деревья решений:

### Как работают:
```
         Площадь <= 70м²?
         /              \
      ДА                НЕТ
      /                  \
Дешёвая            Комнат <= 2?
квартира           /          \
                 ДА           НЕТ
                 /             \
          Средняя цена    Дорогая
```

### Код:
```python
from sklearn.tree import DecisionTreeRegressor

tree = DecisionTreeRegressor(
    max_depth=5,      # ← глубина дерева (3-10)
    random_state=42
)

tree.fit(X_train, y_train)
predictions = tree.predict(X_test)
```

### Параметры:
- **max_depth** - максимальная глубина (обычно 3-10)
- **min_samples_split** - минимум примеров для разделения
- **min_samples_leaf** - минимум примеров в листе

## 🌲 Random Forest:

### Идея:
```
1 дерево = нестабильно
100 деревьев = стабильно (усредняем)

Как спросить совет:
❌ У 1 человека - может ошибиться
✓ У 100 человек - большинство правы
```

### Код:
```python
from sklearn.ensemble import RandomForestRegressor

rf = RandomForestRegressor(
    n_estimators=100,  # ← количество деревьев
    max_depth=5,       # ← глубина каждого
    n_jobs=-1,         # ← использовать все процессоры
    random_state=42
)

rf.fit(X_train, y_train)
predictions = rf.predict(X_test)

# Важность признаков
importances = rf.feature_importances_
```

### Параметры:
- **n_estimators** - количество деревьев (100-500)
- **max_depth** - глубина деревьев (5-10)
- **n_jobs=-1** - параллельные вычисления

## 🔧 Feature Engineering:

### Типы новых признаков:

**1. Комбинации:**
```python
df['Площадь_на_комнату'] = df['Площадь'] / df['Комнаты']
df['Произведение'] = df['Признак1'] * df['Признак2']
```

**2. Преобразования:**
```python
df['Возраст'] = 2024 - df['Год_рождения']
df['Логарифм'] = np.log(df['Значение'] + 1)
```

**3. Категории из чисел:**
```python
df['Высокий'] = (df['Рост'] > 180).astype(int)
df['Группа'] = pd.cut(df['Возраст'], bins=[0, 18, 65, 100], 
                       labels=['Ребёнок', 'Взрослый', 'Пенсионер'])
```

**4. One-Hot Encoding:**
```python
df_encoded = pd.get_dummies(df, columns=['Категория'])

# БЫЛО:
# Цвет
# Красный
# Синий

# СТАЛО:
# Цвет_Красный  Цвет_Синий
# 1             0
# 0             1
```

## ✅ Cross-Validation:

### K-Fold схема:
```
Fold 1: [TEST][train][train][train][train]
Fold 2: [train][TEST][train][train][train]
Fold 3: [train][train][TEST][train][train]
Fold 4: [train][train][train][TEST][train]
Fold 5: [train][train][train][train][TEST]

→ 5 оценок → усредняем
```

### Код:
```python
from sklearn.model_selection import cross_val_score

scores = cross_val_score(
    model,           # модель
    X, y,            # ВСЕ данные
    cv=5,            # количество fold'ов
    scoring='r2'     # метрика
)

print(f"Среднее: {scores.mean():.3f}")
print(f"Разброс: {scores.std():.3f}")
```

### Когда использовать:
- ✓ Мало данных (<1000)
- ✓ Нужна точная оценка
- ✓ Сравнение моделей

## 📊 Сравнение моделей:

| Модель | Плюсы | Минусы | Когда использовать |
|--------|-------|--------|-------------------|
| **Linear Regression** | Быстрая, простая | Только линейные зависимости | Простые данные |
| **Decision Tree** | Понятная, нелинейность | Переобучение | Нужна интерпретация |
| **Random Forest** | Точная, стабильная | Медленная, сложно интерпретировать | Нужна высокая точность |

## 🎯 Полный процесс ML проекта:
```python
# 1. ДАННЫЕ
df = pd.read_csv('data.csv')

# 2. ИЗУЧЕНИЕ
df.describe()
df.plot()

# 3. FEATURE ENGINEERING
df['новый_признак'] = df['признак1'] / df['признак2']
df = pd.get_dummies(df, columns=['категория'])

# 4. ПОДГОТОВКА
X = df.drop('цель', axis=1).values
y = df['цель'].values
X_train, X_test, y_train, y_test = train_test_split(X, y, test_size=0.2)

# 5. ОБУЧЕНИЕ
model = RandomForestRegressor(n_estimators=100)
model.fit(X_train, y_train)

# 6. ОЦЕНКА
y_pred = model.predict(X_test)
r2 = r2_score(y_test, y_pred)

# 7. CROSS-VALIDATION
scores = cross_val_score(model, X, y, cv=5)

# 8. ВАЖНОСТЬ ПРИЗНАКОВ
importances = model.feature_importances_

# 9. ПРИМЕНЕНИЕ
predictions = model.predict(новые_данные)
```

## 💡 Практические советы:

### Feature Engineering:
1. **Начните с простого** - деление, умножение
2. **Подумайте о задаче** - что важно?
3. **Проверяйте важность** - feature_importances_
4. **Удаляйте бесполезные** - упрощайте модель

### Выбор модели:
1. **Начните с простой** - Linear Regression
2. **Попробуйте дерево** - Decision Tree
3. **Используйте лес** - Random Forest
4. **Сравните через CV** - выберите лучшую

### Параметры:
- **max_depth**: начните с 5, увеличивайте если нужно
- **n_estimators**: 100-200 обычно достаточно
- **cv**: используйте 5 для начала

## 🔍 Частые ошибки:

❌ **Не создавать новые признаки** - упускаете улучшение  
❌ **Переобучение дерева** - max_depth слишком большой  
❌ **Не использовать CV** - неточная оценка  
❌ **Забыть One-Hot для категорий** - модель не понимает текст  
❌ **Разные признаки в train/test** - ошибка при предсказании  

## 📈 Метрики:

### Регрессия:
```python
from sklearn.metrics import r2_score, mean_absolute_error, mean_squared_error

r2 = r2_score(y_test, y_pred)              # 0-1, лучше→1
mae = mean_absolute_error(y_test, y_pred)  # меньше→лучше
rmse = np.sqrt(mean_squared_error(...))    # меньше→лучше
```

### Классификация:
```python
from sklearn.metrics import accuracy_score, confusion_matrix

acc = accuracy_score(y_test, y_pred)  # 0-100%
```

## 🎓 Что дальше:

### Следующие техники:
1. **Gradient Boosting** - ещё точнее RF
2. **Hyperparameter Tuning** - GridSearch, RandomSearch
3. **Feature Selection** - выбор важных признаков
4. **Scaling** - нормализация данных
5. **Pipeline** - автоматизация процесса

### Другие модели:
- XGBoost, LightGBM - топовые модели
- Support Vector Machines
- K-Nearest Neighbors
- Neural Networks

## 🏆 Результаты проекта:

**Задача:** Предсказание цен автомобилей

**Данные:** 1000 автомобилей, 6 исходных признаков

**Feature Engineering:** +4 новых признака

**Лучшая модель:** Random Forest
- R² = ~0.95
- MAE = ~50 тыс. руб

**Применение:** Оценка стоимости любого автомобиля

## ✅ Чек-лист освоенных навыков:

- [x] Понимаю как работают деревья решений
- [x] Умею использовать Random Forest
- [x] Могу создавать новые признаки
- [x] Знаю как делать One-Hot Encoding
- [x] Умею использовать Cross-Validation
- [x] Могу сравнивать модели
- [x] Понимаю feature importance
- [x] Могу создать полный ML проект

## 🎯 Следующий день:

**День 26** - Улучшение моделей:
- Hyperparameter tuning
- GridSearch
- Feature selection
- Работа с несбалансированными данными

---

**Поздравляю! Освоены продвинутые техники ML! 🎉**

**Random Forest - одна из самых популярных моделей в индустрии!**