# День 23: Seaborn - красивая статистическая визуализация

## 🎯 Что изучили:

### 1. Основы Seaborn
- Что такое Seaborn (надстройка над Matplotlib)
- Встроенные стили и темы
- Базовые графики с автоматической статистикой
- Работа напрямую с pandas DataFrame

### 2. Основные графики
- **lineplot()** - линейные графики с доверительными интервалами
- **barplot()** - столбчатые диаграммы со статистикой
- **scatterplot()** - диаграммы рассеяния с категориями
- **histplot()** - гистограммы распределения
- **boxplot()** - ящики с усами
- **heatmap()** - тепловые карты корреляций

### 3. Продвинутые графики
- **violinplot()** - распределение в виде скрипки
- **swarmplot()** - все точки данных без перекрытия
- **pointplot()** - точки с доверительными интервалами
- **pairplot()** - матрица взаимосвязей всех переменных
- **jointplot()** - комбинация scatter + распределения
- **countplot()** - автоматический подсчёт категорий
- **FacetGrid** - сетка графиков по категориям

### 4. Работа с реальными данными
- Загрузка встроенных датасетов
- Анализ датасета "tips" (чаевые)
- Создание новых столбцов
- Pivot tables для сводных таблиц
- Группировка и агрегация

### 5. Создание дашбордов
- Компоновка нескольких графиков
- KPI карточки
- Тепловые карты
- Сохранение в файл

## 📁 Файлы:
```
Day 23/
├── 01_seaborn_basics.py        # Основы Seaborn
├── 02_seaborn_advanced.py      # Продвинутые графики
├── 03_real_data_analysis.py    # Анализ реальных данных
├── 04_dashboard.py             # Создание дашборда
├── dashboard.png               # Сохранённый дашборд
└── README.md                   # Этот файл
```

## 🚀 Установка:
```bash
pip install seaborn pandas matplotlib --break-system-packages
```

## 📚 Ключевые концепции:

### Преимущества Seaborn над Matplotlib:
```python
# Matplotlib - много кода
plt.figure(figsize=(10, 6))
plt.scatter(x, y)
plt.xlabel('X')
plt.ylabel('Y')
plt.title('Title')
plt.legend()
plt.grid(True)
plt.show()

# Seaborn - минимум кода
sns.scatterplot(data=df, x='col1', y='col2', hue='category')
plt.show()
```

### Базовая структура:
```python
import seaborn as sns
import matplotlib.pyplot as plt
import pandas as pd

# 1. Применить стиль
sns.set_theme()

# 2. Загрузить данные
df = pd.DataFrame(...)

# 3. Создать график
plt.figure(figsize=(10, 6))
sns.lineplot(data=df, x='x', y='y', hue='category')

# 4. Настроить
plt.title('Заголовок')
plt.xlabel('X')
plt.ylabel('Y')

# 5. Показать
plt.show()
```

## 🎨 Основные графики:

### Для категорий:
```python
sns.barplot(data=df, x='category', y='value')
sns.countplot(data=df, x='category')
sns.boxplot(data=df, x='category', y='value')
sns.violinplot(data=df, x='category', y='value')
```

### Для распределений:
```python
sns.histplot(data=df, x='value', kde=True)
sns.kdeplot(data=df, x='value')
```

### Для связей:
```python
sns.scatterplot(data=df, x='x', y='y')
sns.regplot(data=df, x='x', y='y')
sns.heatmap(correlation_matrix)
```

### Для трендов:
```python
sns.lineplot(data=df, x='time', y='value')
sns.pointplot(data=df, x='category', y='value')
```

## 💡 Ключевые параметры:
```python
# Основные
data=df           # Источник данных (DataFrame)
x='column'        # Столбец для оси X
y='column'        # Столбец для оси Y
hue='category'    # Цвет по категории

# Стилизация
palette='Set1'    # Цветовая палитра
style='category'  # Стиль маркера
size='value'      # Размер по значению

# Статистика
errorbar='sd'     # Стандартное отклонение
ci=95             # Доверительный интервал
estimator='mean'  # Функция агрегации
```

## 🎨 Стили и палитры:
```python
# Стили
sns.set_theme(style='whitegrid')  # Белый + сетка
sns.set_theme(style='darkgrid')   # Тёмный + сетка
sns.set_theme(style='white')      # Белый
sns.set_theme(style='dark')       # Тёмный
sns.set_theme(style='ticks')      # С делениями

# Палитры
sns.set_palette('husl')           # Равномерная
sns.set_palette('Set1')           # Яркие
sns.set_palette('pastel')         # Пастельные
sns.set_palette('deep')           # Глубокие
```

## 📊 Встроенные датасеты:
```python
# Загрузить датасет
tips = sns.load_dataset('tips')
iris = sns.load_dataset('iris')
titanic = sns.load_dataset('titanic')
diamonds = sns.load_dataset('diamonds')
penguins = sns.load_dataset('penguins')

# Посмотреть доступные
print(sns.get_dataset_names())
```

## 🎯 Когда использовать какой график:

| Задача | График | Пример |
|--------|--------|--------|
| Сравнить категории | barplot, countplot | Продажи по месяцам |
| Показать тренд | lineplot, pointplot | Рост акций |
| Распределение | histplot, violinplot | Возраст клиентов |
| Связь переменных | scatterplot, regplot | Цена vs площадь |
| Статистика групп | boxplot, violinplot | Зарплаты по отделам |
| Все точки | swarmplot | Мало данных (<100) |
| Корреляции | heatmap | Связи между столбцами |
| Все связи | pairplot | Быстрый анализ |
| По категориям | FacetGrid | Разбить по группам |

## 💻 Примеры использования:

### Быстрый анализ:
```python
# Одной строкой - полный анализ датасета
sns.pairplot(df, hue='category')
plt.show()
```

### Сравнение групп:
```python
sns.boxplot(data=df, x='category', y='value')
sns.swarmplot(data=df, x='category', y='value', color='black', alpha=0.5)
plt.show()
```

### Тренд с доверительным интервалом:
```python
sns.lineplot(data=df, x='date', y='value', hue='group')
plt.show()
```

### Тепловая карта:
```python
correlation = df.corr()
sns.heatmap(correlation, annot=True, cmap='coolwarm', center=0)
plt.show()
```

## 🎓 Практические советы:

1. **Всегда начинайте с `sns.set_theme()`** - графики сразу красивые
2. **Используйте `hue=` для категорий** - автоматическая раскраска
3. **`pairplot()` для быстрого анализа** - видны все связи
4. **Комбинируйте графики** - box + swarm, scatter + regplot
5. **Сохраняйте в высоком разрешении** - `plt.savefig('file.png', dpi=300)`

## 🔗 Полезные ссылки:

- [Seaborn документация](https://seaborn.pydata.org/)
- [Галерея примеров](https://seaborn.pydata.org/examples/index.html)
- [Палитры цветов](https://seaborn.pydata.org/tutorial/color_palettes.html)

## 🎓 Следующий шаг:

**День 24** - Scikit-learn: введение в машинное обучение

---

## 📊 Сравнение библиотек:

| Библиотека | Уровень | Когда использовать |
|------------|---------|-------------------|
| **Matplotlib** | Низкий | Полный контроль, кастомизация |
| **Seaborn** | Средний | Статистика, быстрые красивые графики |
| **Plotly** | Высокий | Интерактивные графики, веб |
| **Pandas** | Базовый | Быстрые простые графики |

## 🎯 Ключевые выводы:

✅ Seaborn = Matplotlib + красота + статистика  
✅ Минимум кода для профессиональных графиков  
✅ Автоматическая обработка pandas DataFrame  
✅ Встроенные датасеты для практики  
✅ Идеален для data science и анализа данных  

   trgrgrg