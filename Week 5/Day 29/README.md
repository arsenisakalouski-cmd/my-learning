# День 29: Введение в нейронные сети

## 🎯 Что изучили:

### 1. Основы нейронных сетей
- Что такое нейрон (биологический и искусственный)
- Как работает перцептрон (простейшая нейросеть)
- Обучение через примеры
- Почему перцептрон не может XOR

### 2. Многослойные сети (MLP)
- Архитектура: вход → скрытые → выход
- Функции активации (sigmoid, ReLU, tanh)
- Прямое распространение (forward)
- Обратное распространение (backward)
- Решение XOR с помощью MLP

### 3. Keras/TensorFlow
- Простое создание нейросетей
- Sequential модель
- Dense слои
- Компиляция и обучение
- Сохранение моделей

### 4. Практическое применение
- Полный цикл ML с нейросетями
- Масштабирование данных
- Предсказание на новых данных
- Оценка качества

## 📁 Файлы:
```
Day 29/
├── 00_intro.txt                  # Обзор темы
├── 01_perceptron.py             # Перцептрон с нуля
├── 02_multilayer_network.py     # MLP с нуля
├── 03_keras_intro.py            # Первая сеть на Keras
├── 04_simple_example.py         # Практический пример
├── 05_final_test.py             # Итоговый тест
└── README.md                    # Этот файл
```

## 🧠 Перцептрон (1 нейрон):

### Структура:
```
Входы → Веса → Сумма → Активация → Выход
X1 ──┐
X2 ──┤──> Σ ──> f() ──> y
X3 ──┘
```

### Формула:
```python
z = X1*W1 + X2*W2 + X3*W3 + bias
y = activation(z)
```

### Обучение:
```python
W_new = W_old + learning_rate * error * X
error = y_true - y_pred
```

### Что может/не может:
- ✓ AND, OR (линейно разделимые)
- ✗ XOR (нелинейная задача)

### Код:
```python
class Perceptron:
    def __init__(self, n_inputs):
        self.weights = np.random.randn(n_inputs)
        self.bias = np.random.randn()
    
    def predict(self, X):
        z = np.dot(X, self.weights) + self.bias
        return 1 if z >= 0 else 0
    
    def train(self, X, y, epochs=100):
        for epoch in range(epochs):
            for xi, yi in zip(X, y):
                prediction = self.predict(xi)
                error = yi - prediction
                self.weights += learning_rate * error * xi
                self.bias += learning_rate * error
```

## 🧠 MLP (многослойная сеть):

### Архитектура:
```
Вход (3)    Скрытый (4)    Выход (1)
  X1 ──┐       H1 ──┐
       ├──────┤      ├────> Y
  X2 ──┤       H2 ──┤
       │       H3 ──┤
  X3 ──┘       H4 ──┘
```

### Функции активации:

**Sigmoid:**
```python
def sigmoid(z):
    return 1 / (1 + np.exp(-z))
```
- Выход: (0, 1)
- Использование: выходной слой (вероятности)

**ReLU:**
```python
def relu(z):
    return np.maximum(0, z)
```
- Выход: [0, ∞)
- Использование: скрытые слои (популярна!)

**Tanh:**
```python
def tanh(z):
    return np.tanh(z)
```
- Выход: (-1, 1)
- Использование: скрытые слои

### Код (упрощённо):
```python
class MLP:
    def forward(self, X):
        # Скрытый слой
        z1 = X @ W1 + b1
        a1 = sigmoid(z1)
        
        # Выходной слой
        z2 = a1 @ W2 + b2
        a2 = sigmoid(z2)
        
        return a2
    
    def backward(self, X, y, output):
        # Обратное распространение
        output_error = output - y
        output_delta = output_error * sigmoid_derivative(z2)
        
        hidden_error = output_delta @ W2.T
        hidden_delta = hidden_error * sigmoid_derivative(z1)
        
        # Обновление весов
        W2 -= learning_rate * a1.T @ output_delta
        W1 -= learning_rate * X.T @ hidden_delta
```

## 🚀 Keras - простое создание:

### Три шага:
```python
from tensorflow.keras.models import Sequential
from tensorflow.keras.layers import Dense

# 1. СОЗДАТЬ
model = Sequential([
    Dense(8, activation='relu', input_shape=(3,)),  # скрытый
    Dense(4, activation='relu'),                     # скрытый
    Dense(1, activation='sigmoid')                   # выход
])

# 2. НАСТРОИТЬ
model.compile(
    optimizer='adam',
    loss='binary_crossentropy',
    metrics=['accuracy']
)

# 3. ОБУЧИТЬ
history = model.fit(
    X_train, y_train,
    epochs=100,
    batch_size=32,
    validation_split=0.2
)
```

### Основные компоненты:

**Sequential:**
- Модель где слои идут последовательно

**Dense:**
- Полносвязный слой
- Dense(8) = 8 нейронов

**Optimizer:**
- `adam` - самый популярный (умный gradient descent)
- `sgd` - простой gradient descent
- `rmsprop` - альтернатива adam

**Loss:**
- `binary_crossentropy` - для 2 классов (0/1)
- `categorical_crossentropy` - для 3+ классов
- `mse` - для регрессии

**Metrics:**
- `accuracy` - процент правильных
- `mae` - средняя абсолютная ошибка

### Важные параметры:
```python
epochs=100          # сколько раз пройти по данным
batch_size=32       # обновлять веса после каждых 32 примеров
validation_split=0.2  # 20% train → для валидации
verbose=1           # показывать прогресс
```

## 📊 Полный пример (предсказание покупки):
```python
from sklearn.preprocessing import StandardScaler
from sklearn.model_selection import train_test_split

# 1. Данные
X = данные о клиентах (возраст, доход, время)
y = купил или нет (0/1)

# 2. Масштабирование (ВАЖНО!)
scaler = StandardScaler()
X_scaled = scaler.fit_transform(X_train)
X_test_scaled = scaler.transform(X_test)

# 3. Модель
model = Sequential([
    Dense(8, activation='relu', input_shape=(3,)),
    Dense(4, activation='relu'),
    Dense(1, activation='sigmoid')
])

model.compile(
    optimizer='adam',
    loss='binary_crossentropy',
    metrics=['accuracy']
)

# 4. Обучение
history = model.fit(
    X_train, y_train,
    epochs=100,
    batch_size=32,
    validation_split=0.2
)

# 5. Оценка
test_acc = model.evaluate(X_test, y_test)[1]
print(f"Accuracy: {test_acc:.2%}")

# 6. Предсказание
new_data = scaler.transform([[25, 80, 30]])
prediction = model.predict(new_data)
print(f"Вероятность: {prediction[0][0]:.1%}")
```

## 💡 Важные концепции:

### Обратное распространение (простыми словами):

1. **Прямой проход:** данные идут вперёд через слои
2. **Считаем ошибку:** сравниваем выход с правильным ответом
3. **Обратный проход:** ошибка идёт назад, обновляя веса
4. **Повторяем:** много раз пока не научится

### Зачем масштабирование:

**БЕЗ масштабирования:**
- Возраст: 20-70
- Доход: 20000-150000
- Сеть думает что доход важнее!

**С масштабированием:**
- Все признаки: примерно -2 до 2
- Сеть видит все равными

### Переобучение:

**ХОРОШО:**
- Train: 85%, Test: 82% ✓

**ПЛОХО (переобучение):**
- Train: 99%, Test: 65% ✗

**Признаки на графиках:**
- Train loss падает, Validation растёт → переобучение

### Как улучшить модель:

1. **Больше данных** - лучше всего!
2. **Больше эпох** - если не переобучается
3. **Больше нейронов** - осторожно (переобучение)
4. **Dropout** - случайно отключать нейроны (день 30)
5. **Regularization** - штрафовать большие веса (день 30)

## 🎓 Следующие шаги:

**День 30:**
- Улучшение нейросетей (Dropout, Batch Normalization)
- Сохранение и загрузка
- Callbacks
- Early Stopping
- Практические проекты

**Дальше:**
- CNN для изображений
- RNN для текста
- Transfer Learning
- Реальные проекты

## ✅ Чек-лист освоенных навыков:

- [x] Понимаю что такое перцептрон
- [x] Понимаю почему нужны многослойные сети
- [x] Знаю функции активации и когда их использовать
- [x] Понимаю что такое обратное распространение (общая идея)
- [x] Умею создавать модели на Keras
- [x] Знаю процесс: создать → настроить → обучить → оценить
- [x] Понимаю важность масштабирования
- [x] Понимаю что такое переобучение
- [x] Могу предсказывать на новых данных

## 📚 Полезные ресурсы:

- [Keras Documentation](https://keras.io/)
- [TensorFlow Tutorials](https://www.tensorflow.org/tutorials)
- [Neural Networks Playground](https://playground.tensorflow.org/) - интерактивная визуализация

---

**Поздравляю! Вы освоили основы нейронных сетей! 🧠🎉**

**Теперь вы можете создавать свои нейросети для решения реальных задач!**