### День 28 (файл 01): Создание признаков 

**Главная идея:** Новые признаки = новая информация для модели

**5 типов признаков:**

1. **Комбинации** (/, *, +, -)
```python
   df['per_room'] = df['area'] / df['rooms']
```

2. **Логарифмы** - сжать большие значения
```python
   df['log_price'] = np.log1p(df['price'])
```

3. **Степени** - нелинейные зависимости
```python
   df['area_sq'] = df['area'] ** 2
```

4. **Бинарные** (0/1) - да/нет
```python
   df['is_big'] = (df['area'] > 80).astype(int)
```

5. **Группы** - категории из чисел
```python
   df['size_cat'] = pd.cut(df['area'], bins=[0, 50, 100, 200])
```

**Когда что использовать:**
- Логарифм → сильно разные масштабы
- Степени → нелинейная зависимость
- Бинарные → есть важный порог
- Группы → категории важнее чисел



### День 28 (файл 02): Polynomial Features ⭐⭐

**Идея:** Автоматическое создание степеней и комбинаций

**Что делает:**
```python
X = [X1, X2]
degree=2 → [X1, X2, X1², X1×X2, X2²]
```

**Код:**
```python
from sklearn.preprocessing import PolynomialFeatures

poly = PolynomialFeatures(degree=2, include_bias=False)
X_poly = poly.fit_transform(X_train)
X_test_poly = poly.transform(X_test)
```

**Когда использовать:**
- Нелинейная зависимость (парабола, кривая)
- Обычно degree=2, редко 3

**Опасность:** Много признаков → переобучение!

### День 28 (файл 03): Feature Scaling ⭐⭐⭐

**Идея:** Привести все признаки к одному масштабу

**3 метода:**

1. **StandardScaler** (стандартизация)
   - Формула: (x - mean) / std
   - Результат: mean=0, std=1
   - Когда: нормальное распределение

2. **MinMaxScaler** (нормализация)
   - Формула: (x - min) / (max - min)
   - Результат: значения в [0, 1]
   - Когда: нужны значения 0-1

3. **RobustScaler** (устойчивый)
   - Формула: (x - median) / IQR
   - Результат: устойчив к выбросам
   - Когда: есть выбросы

**Код:**
```python
from sklearn.preprocessing import StandardScaler, MinMaxScaler, RobustScaler

# Выбираем метод
scaler = StandardScaler()  # или MinMaxScaler() или RobustScaler()

# Train
X_scaled = scaler.fit_transform(X_train)

# Test
X_test_scaled = scaler.transform(X_test)
```

**Важно:**
- fit_transform() только на train!
- transform() на test!
- Обязательно для: SVM, KNN, Neural Networks
- Не обязательно для: деревья, Random Forest


---

## День 28: Feature Engineering - Итоги ⭐⭐⭐

**Главная идея:** Создание новых признаков = улучшение модели на 10-50%!

**Освоенные техники:**

### 1. Простые комбинации
```python
df['per_room'] = df['area'] / df['rooms']  # деление
df['total'] = df['col1'] * df['col2']      # умножение
df['age'] = 2024 - df['year']              # разность
```

### 2. Логарифмы
```python
df['log_price'] = np.log1p(df['price'])
# Сжимает большие значения, делает распределение нормальным
```

### 3. Степени
```python
df['area_sq'] = df['area'] ** 2  # квадрат
df['area_cube'] = df['area'] ** 3  # куб
```

### 4. Бинарные (0/1)
```python
df['is_big'] = (df['area'] > 100).astype(int)
```

### 5. Группы
```python
df['size_cat'] = pd.cut(df['area'], bins=[0, 50, 100, 200])
```

### 6. Polynomial Features
```python
from sklearn.preprocessing import PolynomialFeatures

poly = PolynomialFeatures(degree=2, include_bias=False)
X_poly = poly.fit_transform(X_train)
X_test_poly = poly.transform(X_test)
```

### 7. Scaling (масштабирование)

**StandardScaler** (стандартизация):
```python
from sklearn.preprocessing import StandardScaler

scaler = StandardScaler()
X_scaled = scaler.fit_transform(X_train)
X_test_scaled = scaler.transform(X_test)
# Результат: mean=0, std=1
```

**MinMaxScaler** (нормализация):
```python
from sklearn.preprocessing import MinMaxScaler

scaler = MinMaxScaler()
X_scaled = scaler.fit_transform(X_train)
# Результат: значения в [0, 1]
```

**RobustScaler** (устойчивый):
```python
from sklearn.preprocessing import RobustScaler

scaler = RobustScaler()
X_scaled = scaler.fit_transform(X_train)
# Устойчив к выбросам
```

### Когда что использовать:

**Логарифм:**
- Сильно разные масштабы (1 до 1000000)
- Скошенное распределение

**Степени:**
- Нелинейная зависимость (парабола)

**Polynomial:**
- Автоматическое создание комбинаций
- degree=2 обычно, 3 редко

**Scaling:**
- StandardScaler: нормальное распределение
- MinMaxScaler: нужны значения 0-1
- RobustScaler: есть выбросы

**Важно для scaling:**
- Обязательно: SVM, KNN, Neural Networks
- Не обязательно: деревья, Random Forest

### Результаты проекта:
- Базовая модель: R² = 0.85
- С Feature Engineering: R² = 0.95
- Улучшение: ~12%

**Вывод:** Feature Engineering = 80% успеха!
```

---

## 📊 ЭТАП 7: ТЕСТ - День 28 (20 мин)

### Вопрос 1: Что делает `np.log1p(x)`?
```
A) log(x)
B) log(1 + x)
C) log(x - 1)
D) x + 1
```

### Вопрос 2: PolynomialFeatures(degree=2) из [X1, X2] создаст:
```
A) [X1, X2]
B) [X1, X2, X1²]
C) [X1, X2, X1², X1×X2, X2²]
D) [X1², X2²]
```

### Вопрос 3: StandardScaler делает:
```
A) (x - min) / (max - min)
B) (x - mean) / std
C) (x - median) / IQR
D) x / max
```

### Вопрос 4: MinMaxScaler результат:
```
A) mean=0, std=1
B) значения в [0, 1]
C) median=0
D) без изменений
```

### Вопрос 5: Когда использовать RobustScaler?
```
A) Всегда
B) Когда есть выбросы
C) Когда нет выбросов
D) Никогда
```

### Вопрос 6: `pd.cut()` используется для:
```
A) Удаления строк
B) Разбиения числа на категории
C) Удаления столбцов
D) Сортировки
```

### Вопрос 7: `(df['age'] > 30).astype(int)` вернёт:
```
A) True/False
B) 0 или 1
C) 30
D) возраст
```

### Вопрос 8: Для каких моделей scaling обязателен?
```
A) Random Forest
B) Decision Tree
C) SVM, KNN, Neural Networks
D) Все модели
```

### Вопрос 9: include_bias=False в PolynomialFeatures означает:
```
A) Не создавать столбец из единиц
B) Не создавать X²
C) Не использовать bias
D) Ничего не значит
```

### Вопрос 10: fit_transform() vs transform():
```
A) Нет разницы
B) fit_transform на train, transform на test
C) transform быстрее
D) fit_transform на test
```

### Вопрос 11: Зачем нужна логарифмическая трансформация?
```
A) Красиво выглядит
B) Сжать большие значения, сделать распределение нормальным
C) Увеличить значения
D) Удалить пропуски
```

### Вопрос 12: df['area'] ** 2 создаёт:
```
A) Удвоение значений
B) Квадрат значений
C) Корень
D) Логарифм
```

### Вопрос 13: Polynomial degree=3 из 2 признаков создаст сколько?
```
A) 3
B) 6
C) 9
D) 10
```

### Вопрос 14: Какой метод scaling устойчив к выбросам?
```
A) StandardScaler
B) MinMaxScaler
C) RobustScaler
D) Никакой
```

### Вопрос 15: Feature Engineering это:
```
A) Удаление признаков
B) Создание новых признаков из существующих
C) Только масштабирование
D) Обучение модели
```

---

## ✅ ОТВЕТЫ:

<details>
<summary>Нажмите чтобы увидеть ответы</summary>

1. **B** - log(1 + x) (чтобы избежать log(0))
2. **C** - [X1, X2, X1², X1×X2, X2²]
3. **B** - (x - mean) / std
4. **B** - значения в [0, 1]
5. **B** - Когда есть выбросы
6. **B** - Разбиения числа на категории
7. **B** - 0 или 1
8. **C** - SVM, KNN, Neural Networks
9. **A** - Не создавать столбец из единиц
10. **B** - fit_transform на train, transform на test
11. **B** - Сжать большие значения, сделать распределение нормальным
12. **B** - Квадрат значений
13. **C** - 9 признаков
14. **C** - RobustScaler
15. **B** - Создание новых признаков из существующих

**ОЦЕНКА:**
- 13-15 правильных: ✅ Отлично! Материал усвоен!
- 10-12 правильных: ⚠️ Хорошо, повторите слабые места
- 7-9 правильных: 📖 Перечитайте файлы ещё раз
- Меньше 7: 🔄 Пройдите день заново

</details>

---

## 🎉 ДЕНЬ 28 ЗАВЕРШЁН!

---

## 📋 ЧТО СДЕЛАЛИ СЕГОДНЯ:
```
✅ Файл 01: Создание признаков (печатали)
✅ Файл 02: Polynomial Features (печатали)
✅ Файл 03: Feature Scaling (печатали)
✅ Файл 04: Финальный проект (печатали)
✅ MY_NOTES.md (заполнили)
✅ ТЕСТ (проверили знания)

