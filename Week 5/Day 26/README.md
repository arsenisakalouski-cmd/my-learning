# День 26: Hyperparameter Tuning - Настройка моделей

## 🎯 Что изучили:

### 1. Гиперпараметры vs Параметры
- **Параметры** - модель находит сама (coef_, intercept_)
- **Гиперпараметры** - мы задаём до обучения (max_depth, n_estimators)
- Влияние гиперпараметров на качество модели
- Ручной подбор параметров

### 2. GridSearchCV
- Автоматический перебор ВСЕХ комбинаций
- Cross-Validation для каждой комбинации
- best_params_, best_score_, best_estimator_
- Тепловые карты результатов

### 3. RandomizedSearchCV
- Случайный поиск параметров
- Быстрее GridSearch (меньше итераций)
- Распределения параметров (randint, uniform)
- Когда использовать вместо GridSearch

### 4. Практический проект
- Реальный датасет (California Housing)
- Полный цикл настройки модели
- Сравнение методов
- Визуализация результатов

## 📁 Файлы:
```
Day 26/
├── 01_hyperparameters_intro.py    # Введение в гиперпараметры
├── 02_grid_search.py              # GridSearchCV
├── 03_randomized_search.py        # RandomizedSearchCV
├── 04_practical_project.py        # Практический проект
└── README.md                      # Этот файл
```

## 🎛️ Гиперпараметры vs Параметры:

### Параметры (автоматические):
```python
model = LinearRegression()
model.fit(X, y)

# Модель САМА находит:
model.coef_        # коэффициенты
model.intercept_   # свободный член

# МЫ ИХ НЕ ТРОГАЕМ!
```

### Гиперпараметры (ручные):
```python
# МЫ задаём ДО обучения:
model = RandomForestRegressor(
    n_estimators=100,    # ← гиперпараметр
    max_depth=10,        # ← гиперпараметр
    min_samples_split=5  # ← гиперпараметр
)

model.fit(X, y)
# Модель использует наши настройки
```

## 🔍 GridSearchCV:

### Как работает:
```
Задаём сетку:
n_estimators: [50, 100, 200]
max_depth: [5, 10, 15]

GridSearch пробует ВСЕ 9 комбинаций:
(50, 5), (50, 10), (50, 15)
(100, 5), (100, 10), (100, 15)
(200, 5), (200, 10), (200, 15)

Для КАЖДОЙ делает CV → выбирает лучшую
```

### Код:
```python
from sklearn.model_selection import GridSearchCV

# Сетка параметров
param_grid = {
    'n_estimators': [50, 100, 200],
    'max_depth': [5, 10, 15],
    'min_samples_split': [2, 5, 10]
}

# GridSearch
grid_search = GridSearchCV(
    estimator=RandomForestRegressor(random_state=42),
    param_grid=param_grid,
    cv=5,              # кросс-валидация
    scoring='r2',
    verbose=1,
    n_jobs=-1          # все процессоры
)

# Поиск
grid_search.fit(X_train, y_train)

# Результаты
print(grid_search.best_params_)      # лучшие параметры
print(grid_search.best_score_)       # лучший CV score
best_model = grid_search.best_estimator_  # готовая модель
```

### Ключевые атрибуты:
```python
# После fit():
grid_search.best_params_      # {'n_estimators': 100, 'max_depth': 10, ...}
grid_search.best_score_       # 0.8456 (CV R²)
grid_search.best_estimator_   # готовая модель
grid_search.cv_results_       # детальные результаты всех комбинаций
```

## 🎲 RandomizedSearchCV:

### Идея:
```
Вместо ВСЕХ комбинаций → N случайных

GridSearch: 10 × 10 × 10 = 1000 комбинаций
RandomizedSearch: 50 случайных комбинаций

Быстрее, обычно находит хорошие параметры!
```

### Код:
```python
from sklearn.model_selection import RandomizedSearchCV
from scipy.stats import randint, uniform

# Распределения (не конкретные значения!)
param_distributions = {
    'n_estimators': randint(50, 300),      # случайное целое от 50 до 300
    'max_depth': randint(5, 50),           # случайное целое от 5 до 50
    'max_features': uniform(0.1, 0.9)      # случайное дробное от 0.1 до 1.0
}

# RandomizedSearch
random_search = RandomizedSearchCV(
    estimator=RandomForestRegressor(random_state=42),
    param_distributions=param_distributions,
    n_iter=50,         # сколько попыток
    cv=5,
    scoring='r2',
    random_state=42,
    n_jobs=-1
)

random_search.fit(X_train, y_train)

# Те же атрибуты что у GridSearch
print(random_search.best_params_)
best_model = random_search.best_estimator_
```

### Распределения:
```python
from scipy.stats import randint, uniform

# Целые числа
randint(50, 300)     # случайное от 50 до 299

# Дробные числа
uniform(0.1, 0.9)    # случайное от 0.1 до 1.0

# Список значений
['sqrt', 'log2', None]  # случайное из списка
```

## 📊 Сравнение методов:

| Метод | Преимущества | Недостатки | Когда использовать |
|-------|-------------|-----------|-------------------|
| **Ручной подбор** | Понимание процесса | Долго, неэффективно | Обучение |
| **GridSearchCV** | Находит лучшее, точно | Медленно для больших сеток | Мало параметров (2-3) |
| **RandomizedSearchCV** | Быстро, часто достаточно | Может пропустить лучшее | Много параметров (4+) |

## 🎯 Стратегия настройки:

### Комбинированный подход:
```python
# ШАГ 1: RandomizedSearch (грубая настройка)
param_distributions = {
    'n_estimators': randint(50, 500),
    'max_depth': randint(5, 50),
    'min_samples_split': randint(2, 20)
}

random_search = RandomizedSearchCV(..., n_iter=50)
random_search.fit(X_train, y_train)

best_n = random_search.best_params_['n_estimators']
best_d = random_search.best_params_['max_depth']

# ШАГ 2: GridSearch (точная настройка вокруг найденного)
param_grid = {
    'n_estimators': [best_n - 50, best_n, best_n + 50],
    'max_depth': [best_d - 5, best_d, best_d + 5],
    'min_samples_split': [2, 5, 10]
}

grid_search = GridSearchCV(...)
grid_search.fit(X_train, y_train)

# Финальная модель
final_model = grid_search.best_estimator_
```

## 🔧 Основные гиперпараметры Random Forest:
```python
RandomForestRegressor(
    n_estimators=100,       # количество деревьев
                           # больше = точнее, медленнее
                           # обычно: 100-500
    
    max_depth=10,          # глубина деревьев
                           # маленькая = недообучение
                           # большая = переобучение
                           # обычно: 5-20
    
    min_samples_split=2,   # минимум примеров для разделения
                           # больше = проще дерево
                           # обычно: 2-20
    
    min_samples_leaf=1,    # минимум примеров в листе
                           # больше = проще дерево
                           # обычно: 1-10
    
    max_features='sqrt',   # признаков в каждом дереве
                           # 'sqrt' - корень из количества
                           # 'log2' - логарифм
                           # None - все
    
    random_state=42,       # для воспроизводимости
    n_jobs=-1              # параллельные вычисления
)
```

## 📈 Результаты проекта:

**Задача:** Предсказание цен домов (California Housing)

**Данные:** 20,640 домов, 8 признаков

| Модель | R² | MAE | Время |
|--------|-----|-----|-------|
| Базовая | 0.8012 | $0.33 | 2.5с |
| RandomizedSearch | 0.8156 | $0.31 | 95.3с |
| GridSearch | 0.8187 | $0.30 | 127.8с |

**Улучшение:** +2.2% точности через настройку

## 💡 Практические советы:

### Когда использовать GridSearch:
- ✓ 2-3 параметра
- ✓ Небольшие диапазоны
- ✓ Финальная настройка
- ✓ Есть время

### Когда использовать RandomizedSearch:
- ✓ 4+ параметров
- ✓ Широкие диапазоны
- ✓ Первичная настройка
- ✓ Ограниченное время

### Оптимизация времени:
```python
# Уменьшить CV
cv=3  # вместо 5

# Меньше итераций
n_iter=30  # вместо 100

# Параллельные вычисления
n_jobs=-1  # все процессоры

# Меньше данных для экспериментов
X_sample = X_train[:5000]  # только часть
```

## ⚠️ Частые ошибки:

❌ **Тестировать на тех же данных что для CV**
```python
# НЕПРАВИЛЬНО:
grid_search.fit(X, y)  # использует ВСЕ данные
score = grid_search.score(X, y)  # те же данные!

# ПРАВИЛЬНО:
grid_search.fit(X_train, y_train)
score = grid_search.score(X_test, y_test)  # новые данные
```

❌ **Слишком большая сетка**
```python
# 10 × 10 × 10 × 10 = 10,000 комбинаций!
# Будет идти часами
```

❌ **Забыть n_jobs=-1**
```python
# Медленно (один процессор)
GridSearchCV(..., n_jobs=1)

# Быстро (все процессоры)
GridSearchCV(..., n_jobs=-1)
```

## 🎓 Следующие шаги:

1. **Другие методы оптимизации:**
   - Bayesian Optimization
   - Genetic Algorithms
   - Optuna

2. **Feature Selection:**
   - Удаление неважных признаков
   - Recursive Feature Elimination

3. **Ensemble методы:**
   - Stacking
   - Blending
   - Voting

## ✅ Чек-лист освоенных навыков:

- [x] Понимаю разницу параметры vs гиперпараметры
- [x] Умею использовать GridSearchCV
- [x] Умею использовать RandomizedSearchCV
- [x] Знаю когда что использовать
- [x] Могу настроить Random Forest
- [x] Понимаю комбинированный подход
- [x] Умею оптимизировать время поиска

## 🎯 Следующий день:

**День 27** - Работа с реальными данными:
- Очистка данных
- Feature Selection
- Обработка выбросов
- Несбалансированные классы

---

**Поздравляю! Научились настраивать модели как профессионалы! 🎉**

**Hyperparameter Tuning - ключевой навык Data Scientist!**